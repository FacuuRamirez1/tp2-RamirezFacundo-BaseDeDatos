# AGENTS.md

## Estado del repo

Proyecto universitario de **Base de Datos 2 (TP2)** en etapa de scaffold inicial. Solo existen 3 archivos trackeados (`README.md`, `.env.example`, `.gitignore`), los tres vacíos, y carpetas de destino previstas.

No hay build system, tests, linter, CI, ni manifiesto de dependencias. No intentes buscar configs que no existen (`npm`, `cargo`, `requirements.txt`, etc.).

## Estructura prevista (verificable, no implementada)

- `src/` — código fuente (vacía)
- `db/backups/` — backups de base de datos (existe, vacía)
- `docs/` — documentación (vacía)
- `.env.example`, `README.md` — placeholder, ambos sin contenido

## Setup

- Paquete: `tp2-RamirezFacundo-BaseDeDatos` (`origin` en GitHub, rama `main`).
- `.env.example` / `.gitignore` existen pero aún no definen variables ni reglas.

## Convenciones

- No hay convenciones establecidas todavía. Al introducir código, consultar al usuario antes de asumir un lenguaje, ORM/sistema de base de datos o stack, ya que nada está decidido.

## Seguridad

Respetar siempre las normas definidas en `.kiro/steering/security-policies.md`.
